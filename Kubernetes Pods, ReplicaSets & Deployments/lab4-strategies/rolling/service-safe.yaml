apiVersion: v1
kind: Service
metadata:
  name: app-rolling-safe-service
  labels:
    app: app-rolling-safe
spec:
  type: NodePort
  selector:
    app: app-rolling-safe
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
